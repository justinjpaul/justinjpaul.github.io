import Typography from "@mui/joy/Typography";

export default function NotFoundPage() {
  return (
    <Typography level="h2">
      Page not found. Check out my pages by clicking on the buttons on the nav
      bar at the top :)
    </Typography>
  );
}
